# =============================================================================
# 随手学 kb-mcp 环境变量（示例）
#
# 环境变量会覆盖 config.yaml 中的同名设置。
# 使用前请复制为 .env（.env 已在 .gitignore 中排除）：
#   cp .env.example .env
# =============================================================================

# --- Lite 版（基础设置）---
SUISHOUXUE_CARDS_DIR=./cards
SUISHOUXUE_PROFILES_DIR=../profiles
SUISHOUXUE_DEFAULT_PROFILE=general
